#include<stdio.h>
void main(){
    int a,b,x;
    x = scanf("%d %d %d",&a,&b);            // not printing//   //reason:it will get the input from the user but will not return any value of x//
    printf("x is : %d",x);
}